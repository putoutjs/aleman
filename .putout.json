{
    "rules": {
        "putout": "on",
        "convert-array-copy-to-slice": "off"
    },
    "match": {
        "addons/**/*.js": {
            "nodejs": "off"
        },
        "addons/**/*.spec.js": {
            "nodejs": "on"
        }
    }
}
